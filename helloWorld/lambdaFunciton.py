# lamba Function --> Function written in one line using "lambda" keyword.
#                    Accepts any number of argumenmts, but only has one expression

# lambda parameters:expression

square = lambda x: x**2

print(square(4))

